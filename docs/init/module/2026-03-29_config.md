# Config 模块设计文档

> **模板版本**: 3.0
> **创建日期**: 2026-03-29
> **最后更新**: 2026-03-31

---

## 1. 概述

### 1.1 模块名称

Config（配置管理）

### 1.2 职责

Config 模块是 Mozi 系统的核心基础设施层组件，负责：
- 管理应用配置（JSON 格式）
- 配置加载与校验（JSON Schema 验证）
- 环境变量注入（运行时动态替换）
- 配置热更新（无需重启应用）

### 1.3 核心能力

| 能力 | 说明 |
| ---- | ---- |
| JSON 配置加载 | 从指定路径加载 JSON 配置文件 |
| Schema 校验 | 使用 JSON Schema 验证配置合法性 |
| 环境变量注入 | 支持 `${ENV_VAR}` 语法注入环境变量 |
| 热更新 | 监听文件变化，自动重载配置 |
| 多环境支持 | 支持 development/staging/production 环境配置 |
| 配置合并 | 支持 base + override 配置合并策略 |

---

## 2. 核心问题与解决方案

### 2.1 配置格式不统一与校验缺失

**问题描述**：不同环境、不同模块使用不同配置格式，缺乏统一规范和校验机制。当配置错误时，错误往往在运行时才被发现，难以定位根因。

**挑战**：配置结构复杂，Schema 需要覆盖所有边界情况，同时要保持足够的灵活性。传统错误处理方式难以覆盖所有配置场景。

**解决方案**：采用 JSON Schema Draft 7 标准进行配置验证，支持嵌套引用和自定义规则。所有配置必须通过 Schema 校验才能加载，校验失败时抛出明确的错误信息和错误位置。

### 2.2 敏感信息泄露风险

**问题描述**：密钥等敏感信息直接写在配置文件中，存在安全风险。环境变量注入时需要避免注入攻击，确保变量替换后格式正确。

**挑战**：需要在安全性和易用性之间取得平衡。环境变量注入过程可能被恶意利用。

**解决方案**：统一使用 `${ENV_VAR}` 和 `${ENV_VAR:default}` 语法进行环境变量注入，在加载时进行格式验证和安全检查。敏感配置通过环境变量注入，不存储在配置文件中。

### 2.3 配置不可热更新

**问题描述**：修改配置需要重启应用，影响系统可用性。配置重载过程中可能出现不一致状态。

**挑战**：热更新机制需要保证原子性，避免在重载过程中出现不一致状态。同时要考虑配置文件不存在或格式错误的情况。

**解决方案**：使用 mtime 监测文件变化，验证通过后原子性重载配置。新配置校验通过后才生效，失败时使用旧配置继续运行，通过 EventBus 通知变更。

### 2.4 多环境配置切换复杂

**问题描述**：不同环境（development/staging/production）的配置差异需要有效管理，避免在不同环境间切换时出现配置错误。

**挑战**：需要在不同环境间无缝切换，同时保持配置的复用性和可维护性。

**解决方案**：支持 base + override 配置合并策略，base 配置包含通用设置，override 配置包含环境特定设置。深度合并算法确保环境特定配置正确覆盖基础配置。

---

## 3. 数据模型与状态机

### 3.1 核心类型定义

```python
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any


class ConfigError(Exception):
    """Config 模块基础异常"""

    def __init__(self, code: str, message: str) -> None:
        self.code = code
        super().__init__(message)


class ConfigValidationError(ConfigError):
    """配置校验失败"""
    def __init__(self, message: str, errors: list[str] | None = None) -> None:
        super().__init__("Cfg_001", f"Validation failed: {message}")
        self.errors = errors or []


class ConfigNotFoundError(ConfigError):
    """配置文件不存在"""
    def __init__(self, path: str) -> None:
        super().__init__("Cfg_002", f"Config file not found: {path}")


class ConfigLoadError(ConfigError):
    """配置加载失败"""
    def __init__(self, path: str, cause: str) -> None:
        super().__init__("Cfg_003", f"Failed to load config from {path}: {cause}")


@dataclass
class AppConfig:
    """应用配置"""
    version: str
    environment: str = "development"
    app: dict[str, Any] = field(default_factory=dict)
    model: dict[str, Any] = field(default_factory=dict)
    session: dict[str, Any] = field(default_factory=dict)
    tools: dict[str, Any] = field(default_factory=dict)
    security: dict[str, Any] = field(default_factory=dict)

    def get(self, key: str, default: Any = None) -> Any:
        """获取配置值，支持点号分隔路径"""
        keys = key.split(".")
        value = self.__dict__
        for k in keys:
            if isinstance(value, dict):
                value = value.get(k)
            else:
                return default
            if value is None:
                return default
        return value
```

### 3.2 主配置结构

```json
{
    "version": "1.0",
    "environment": "development",
    "app": {
        "name": "mozi",
        "debug": true,
        "log_level": "DEBUG"
    },
    "model": {
        "provider": "anthropic",
        "model": "claude-3-5-sonnet-20241022",
        "temperature": 0.7,
        "max_tokens": 8192,
        "api_base": "https://api.anthropic.com"
    },
    "session": {
        "storage_dir": "~/.mozi/sessions",
        "max_age_days": 30,
        "idle_timeout_seconds": 3600,
        "context_window": {
            "max_tokens": 100000,
            "max_messages": 1000
        }
    },
    "tools": {
        "timeout_seconds": 30,
        "path_whitelist": ["."],
        "allowed_commands": ["ls", "cat", "grep", "find"]
    },
    "security": {
        "sandbox_enabled": true,
        "max_file_size_mb": 100
    }
}
```

---

## 4. 模块结构

### 4.1 目录结构

```
mozi/capabilities/config/
    __init__.py              # 模块导出
    loader.py                 # 配置加载器核心
    schemas/                  # JSON Schema 定义
        __init__.py
        agents.py             # Agent 相关配置 Schema
        tools.py              # Tools 相关配置 Schema
        config.py             # 主配置 Schema
```

### 4.2 关键文件

| 文件 | 职责 |
| ---- | ---- |
| `loader.py` | 配置加载器核心实现、Schema 校验、环境变量注入、热更新 |
| `schemas/config.py` | 主配置 JSON Schema |
| `schemas/agents.py` | Agent 配置 Schema |
| `schemas/tools.py` | Tools 配置 Schema |

---

## 5. 接口、交互与流程

### 5.1 核心接口定义

```python
from abc import ABC, abstractmethod


class BaseConfigLoader(ABC):
    """配置加载器抽象基类"""

    @abstractmethod
    async def load(self, config_path: str | Path) -> AppConfig:
        """加载配置"""
        raise NotImplementedError

    @abstractmethod
    async def validate(self, config: dict[str, Any]) -> list[str]:
        """校验配置，返回错误列表"""
        raise NotImplementedError


class ConfigAccessor:
    """配置访问器（单例模式）"""

    _instance: "ConfigAccessor | None" = None

    def __init__(self, config: AppConfig) -> None:
        self._config = config

    @classmethod
    def initialize(cls, config: AppConfig) -> None:
        """初始化配置访问器"""
        cls._instance = cls(config)

    @classmethod
    def get_instance(cls) -> AppConfig:
        """获取配置实例"""
        if cls._instance is None:
            raise ConfigError("Cfg_004", "Config not initialized")
        return cls._instance._config

    @classmethod
    def get(cls, key: str, default: Any = None) -> Any:
        """获取配置值"""
        return cls.get_instance().get(key, default)
```

### 5.2 模块交互图

```
                    Config 模块
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
    ┌────────┐    ┌──────────┐    ┌──────────┐
    │CLI/    │    │Orchestra-│    │  Model   │
    │Ingress │    │   tor    │    │ Adapter  │
    └────┬───┘    └────┬─────┘    └────┬─────┘
         │             │               │
         │ config.load │               │
         │ ──────────► │               │
         │             │ model.get()   │
         │             │ ─────────────►│
         │             │               │
         ▼             ▼               ▼
    ┌─────────────────────────────────────────┐
    │              EventBus                    │
    │  config_changed 事件发布/订阅              │
    └─────────────────────────────────────────┘
```

### 5.3 配置加载时序

```
用户/启动
    │
    ▼
CLI.main()
    │
    ▼
ConfigLoader.load("config.json")
    │
    ├──► 读取 JSON 文件
    │
    ├──► inject_env_vars() ──► 环境变量注入
    │
    ├──► Schema 校验
    │
    └──► AppConfig 对象创建
            │
            ▼
ConfigAccessor.initialize(config)
            │
            ▼
Orchestrator(orch_config=config)
            │
            ▼
各模块初始化，读取所需配置项
```

---

## 6. 边界与契约

### 6.1 错误码定义

| 错误码 | 说明 | 触发场景 |
| ------ | ---- | -------- |
| `Cfg_001` | 配置校验失败 | JSON Schema 校验不通过 |
| `Cfg_002` | 配置文件不存在 | 指定的配置文件路径不存在 |
| `Cfg_003` | 配置加载失败 | 文件读取或解析失败 |
| `Cfg_004` | 配置未初始化 | 尝试访问未初始化的配置 |
| `Cfg_005` | 配置键不存在 | 尝试获取不存在的配置键 |
| `Cfg_006` | 环境变量替换失败 | 无法解析 `${ENV_VAR}` 语法 |
| `Cfg_007` | 配置合并失败 | base 和 override 配置合并失败 |

### 6.2 API 契约

#### 6.2.1 加载配置

```
POST /config/load
Content-Type: application/json

Request:
{
    "path": "string",         // 配置文件路径
    "validate": true,         // 是否进行 Schema 校验
    "inject_env": true        // 是否注入环境变量
}

Response:
{
    "config": {...},
    "warnings": []
}
```

#### 6.2.2 获取配置值

```
GET /config/get?key=app.log_level

Response:
{
    "key": "app.log_level",
    "value": "DEBUG",
    "exists": true
}
```

#### 6.2.3 热更新配置

```
POST /config/reload

Response:
{
    "status": "reloaded",
    "previous_version": "1.0",
    "new_version": "1.0",
    "changed_keys": ["app.log_level"]
}
```

---

## 7. 实现细节

### 7.1 环境变量注入

```python
import os
import re

ENV_VAR_PATTERN = re.compile(r"\$\{([^}:]+)(?::([^}]*))?\}")


def inject_env_vars(config: dict[str, Any]) -> dict[str, Any]:
    """递归注入环境变量到配置"""
    result = {}

    for key, value in config.items():
        if isinstance(value, str):
            result[key] = _replace_env_var(value)
        elif isinstance(value, dict):
            result[key] = inject_env_vars(value)
        elif isinstance(value, list):
            result[key] = [
                _replace_env_var(item) if isinstance(item, str) else item
                for item in value
            ]
        else:
            result[key] = value

    return result


def _replace_env_var(value: str) -> str:
    """替换单个字符串中的环境变量"""
    def replacer(match: re.Match) -> str:
        env_var = match.group(1)
        default = match.group(2)
        return os.environ.get(env_var, default or match.group(0))

    return ENV_VAR_PATTERN.sub(replacer, value)
```

### 7.2 配置合并策略

```python
def merge_configs(base: dict[str, Any], override: dict[str, Any]) -> dict[str, Any]:
    """深度合并配置"""
    result = base.copy()

    for key, value in override.items():
        if key in result and isinstance(result[key], dict) and isinstance(value, dict):
            result[key] = merge_configs(result[key], value)
        else:
            result[key] = value

    return result
```

### 7.3 热更新机制

```
    配置文件
        │
        ▼
    ┌────────────┐
    │ FileWatcher │ ──► 定期检查 mtime
    └─────┬──────┘
          │
          │ mtime 发生变化
          ▼
    ┌────────────┐
    │  触发重载   │
    └─────┬──────┘
          │
          ▼
    ┌────────────┐
    │ 重新加载配置│
    │  Schema 校验│
    └─────┬──────┘
          │
          ▼
    ┌────────────┐
    │ 通知变更   │
    │ EventBus   │
    └────────────┘
```

---

## 8. 配置

本文档汇总了系统所有模块的配置项。详细测试策略见 [testing.md](./2026-03-29_testing.md)。

### 8.1 全局配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `version` | string | - | 配置版本（必填） |
| `environment` | string | `development` | 运行环境 |

### 8.2 App 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `app.debug` | boolean | `false` | 调试模式 |
| `app.log_level` | string | `INFO` | 日志级别 |

### 8.3 CLI / Ingress 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `cli.repl_history` | string | `~/.mozi/history` | REPL 历史文件路径 |
| `cli.repl_prompt` | string | `"mozi> "` | REPL 提示符 |
| `cli.default_timeout` | integer | `300` | 默认超时时间（秒） |
| `cli.max_retries` | integer | `3` | 最大重试次数 |
| `cli.output_color` | boolean | `true` | 是否启用彩色输出 |
| `cli.verbose` | boolean | `false` | 默认详细输出 |

### 8.4 Session 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `session.db_path` | string | `~/.mozi/sessions.db` | SQLite 数据库路径 |
| `session.attachment_dir` | string | `~/.mozi/attachments` | 附件存储目录 |
| `session.storage_dir` | string | `~/.mozi/sessions` | 会话存储目录 |
| `session.tool_call_file_threshold` | integer | `4096` | 工具调用结果存文件阈值（字节） |
| `session.max_age_days` | integer | `30` | 会话最大保留天数 |
| `session.idle_timeout_seconds` | integer | `3600` | 空闲超时时间（秒） |
| `session.auto_save_interval_seconds` | integer | `30` | 自动保存间隔（秒） |
| `session.auto_save_message_count` | integer | `1` | 自动保存消息数量阈值（1=实时保存） |
| `session.context_window.max_tokens` | integer | `100000` | 最大上下文 Token |
| `session.context_window.max_messages` | integer | `1000` | 最大消息数 |

### 8.5 Context 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `context.window_messages` | integer | `10` | 保留的对话轮数 |
| `context.max_message_length` | integer | `2000` | 单条消息最大长度 |
| `context.max_tokens` | integer | `16000` | 上下文总 token 上限 |
| `context.max_memories` | integer | `5` | 召回记忆数量 |
| `context.push_ratio` | float | `0.3` | Push (Static Preload) 的 token 占比 |
| `context.compress_threshold` | integer | `12000` | 触发压缩的 token 阈值 |
| `context.offload_threshold` | integer | `14000` | 触发 offload 的工具结果大小阈值 |
| `context.isolate_threshold` | integer | `15000` | 触发 Isolate 的任务复杂度阈值 |
| `context.clear_at_least` | integer | `2000` | Context Editing 最小清除 token 数 |

### 8.6 Memory 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `memory.vector_store` | string | `qdrant` | 向量存储后端 |
| `memory.embedding_model` | string | `text-embedding-3-small` | Embedding 模型 |
| `memory.embedding_dimension` | integer | `1536` | Embedding 维度 |
| `memory.short_term.window_size` | integer | `10` | 滑动窗口大小 |
| `memory.short_term.max_tokens` | integer | `8000` | 最大 token 数 |
| `memory.retrieval.limit` | integer | `10` | 召回数量 |
| `memory.similarity_threshold` | float | `0.7` | 检索相似度阈值 |
| `memory.duplication_threshold` | float | `0.85` | 合并阈值，超过则合并 |

### 8.7 Tools 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `tools.default_timeout` | integer | `30` | 默认超时时间（秒） |
| `tools.max_output_size` | integer | `10000` | 最大输出大小 |
| `tools.allowed_paths` | list | `["."]` | 允许的文件路径 |
| `tools.lsp.enabled` | boolean | `true` | 是否启用 LSP |
| `tools.lsp.servers` | dict | `{}` | LSP 服务器配置 |
| `tools.web_search.enabled` | boolean | `true` | 是否启用 Web 检索 |
| `tools.web_fetch.enabled` | boolean | `true` | 是否启用 WebFetch |
| `tools.web_fetch.max_size` | integer | `10000` | WebFetch 最大内容大小 |
| `tools.web_fetch.blocked_hosts` | list | `[]` | 禁止访问的主机列表 |
| `tools.skills.enabled` | boolean | `true` | 是否启用 Skills |
| `tools.task.enabled` | boolean | `true` | 是否启用任务工具 |

### 8.8 Storage 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `storage.backend` | string | `file_system` | 存储后端类型 |
| `storage.base_path` | string | `~/.mozi/storage` | 基础存储路径 |
| `storage.max_size_mb` | integer | `1024` | 最大存储大小（MB） |
| `storage.auto_cleanup` | boolean | `true` | 是否自动清理 |
| `storage.cleanup_interval_hours` | integer | `24` | 清理间隔（小时） |
| `storage.retention_days` | integer | `30` | 数据保留期限（天） |

### 8.9 Resilience 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `rate_limit.global` | integer | `1000` | 全局限流阈值 |
| `rate_limit.per_user` | integer | `10` | 用户级限流阈值 |
| `circuit_breaker.failure_threshold` | integer | `5` | 熔断触发失败次数 |
| `circuit_breaker.success_threshold` | integer | `2` | 恢复成功次数 |
| `circuit_breaker.timeout` | float | `30.0` | OPEN 状态持续时间（秒） |
| `retry.max_attempts` | integer | `3` | 最大重试次数 |
| `retry.base_delay` | float | `1.0` | 重试基础延迟（秒） |
| `retry.max_delay` | float | `60.0` | 重试最大延迟（秒） |
| `timeout.connect` | float | `10.0` | 连接超时（秒） |
| `timeout.read` | float | `30.0` | 读取超时（秒） |
| `timeout.request` | float | `60.0` | 请求超时（秒） |
| `timeout.task` | float | `300.0` | 任务超时（秒） |

### 8.10 Task 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `task.decompose.max_subtasks` | integer | `10` | 最大子任务数量 |
| `task.decompose.min_subtasks` | integer | `1` | 最小子任务数量 |
| `task.retry.max_attempts` | integer | `3` | 最大重试次数 |
| `task.retry.base_delay` | float | `1.0` | 重试基础延迟（秒） |
| `task.retry.max_delay` | float | `60.0` | 重试最大延迟（秒） |
| `task.timeout.task_seconds` | integer | `1800` | 任务级超时（秒） |
| `task.timeout.subtask_seconds` | integer | `300` | 子任务级超时（秒） |
| `task.rollback.enabled` | boolean | `true` | 是否启用回滚 |
| `task.degrade.enabled` | boolean | `true` | 是否启用降级 |

### 8.11 Error Handling 配置

| 配置项 | 类型 | 默认值 | 说明 |
| ------ | ---- | ------ | ---- |
| `error.max_retries` | integer | `3` | 最大重试次数 |
| `error.retry_backoff` | string | `exponential` | 退避策略 |
| `error.loop.max_iterations` | integer | `20` | 最大迭代次数 |
| `error.hallucination.threshold` | float | `0.7` | 置信度阈值 |
| `error.uncaught_handler` | string | `log_and_raise` | 未捕获异常处理 |

### 8.12 环境变量映射

| 环境变量 | 对应配置项 | 说明 |
| -------- | ---------- | ---- |
| `ANTHROPIC_API_KEY` | `model.api_key` | Anthropic API 密钥 |
| `ANTHROPIC_API_BASE` | `model.api_base` | Anthropic API 地址 |
| `OPENAI_API_KEY` | `model.api_key` | OpenAI API 密钥 |
| `MOZI_LOG_LEVEL` | `app.log_level` | 日志级别 |
| `MOZI_DEBUG` | `app.debug` | 调试模式 |

---

## 9. 度量指标

### 9.1 核心指标

| 指标名称 | 类型 | 标签 | 说明 |
| -------- | ---- | ---- | ---- |
| `config_load_total` | Counter | status | 配置加载次数 |
| `config_load_duration_seconds` | Histogram | - | 配置加载耗时 |
| `config_validation_errors_total` | Counter | error_type | 配置校验错误次数 |
| `config_reload_total` | Counter | status | 配置热更新次数 |
| `config_change_notifications_total` | Counter | - | 配置变更通知次数 |

### 9.2 指标采集状态

**状态**：待定义

---

## 10. 参考

- **错误处理**：遵循统一异常体系，见 [error_handling.md](./2026-03-29_error_handling.md)
- **测试策略**：见 [testing.md](./2026-03-29_testing.md)
- **相关模块**：EventBus、[EventBus](./2026-03-29_eventbus.md)

---

## 变更记录

| 版本 | 日期 | 变更内容 |
| ---- | ---- | -------- |
| 3.0 | 2026-03-31 | 重组为模板 v3.0 结构，新增 §6 边界与契约、§9 度量指标 |
| 2.1 | 2026-03-31 | 合并所有模块配置项到本文档，按模块分组重构配置章节结构 |
| 2.0 | 2026-03-30 | 重构为模板 v2.0 结构，核心问题改为段落描述 |
| 1.0 | 2026-03-29 | 初始版本 |

_版本: 3.0_
_更新日期: 2026-03-31_
